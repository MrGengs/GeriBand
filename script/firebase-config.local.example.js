/* ============================================================
   GERIBAND — Firebase Project Credentials (TEMPLATE)
   Copy this file to `firebase-config.local.js` (same folder)
   and replace the placeholders with your Firebase Web App config.
   The real `firebase-config.local.js` is gitignored.
   ============================================================ */

window.__FIREBASE_CONFIG__ = {
  apiKey:            "YOUR_API_KEY",
  authDomain:        "YOUR_PROJECT.firebaseapp.com",
  projectId:         "YOUR_PROJECT_ID",
  storageBucket:     "YOUR_PROJECT.firebasestorage.app",
  messagingSenderId: "YOUR_SENDER_ID",
  appId:             "YOUR_APP_ID",
};
